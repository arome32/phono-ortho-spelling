from glob import glob
import os


# shuffle sets

# get_next_set
    # pick word
        # if correct spelling: next_set
        # if incorrect spelling: next word in set
        # next set
    
def generate_sets(sets):
    for myset in sets:
        word = 
        if kk
        yield myset

gen = generate_nouns(nouns)
